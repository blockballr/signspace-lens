# Round B9 — Investigate Desktop Preview keyboard input path

Owner voice. Run this in CLAD, then report back with evidence.

## Context

The verified typed-input loop works (B7: `testPhrase` injection proved
`heard() → playText() → readout`). But there is no visible, working place to type in
the Desktop Preview: the HUD creates a `TextInputField` (`SignInput`), yet the
on-screen system keyboard does not appear and keystrokes do not register in Preview.
B7 worked around this by injecting a phrase through an inspector field instead of a
real type-in.

Before I choose between clickable vocabulary buttons vs. a live text field, I need
you to investigate what the Desktop Preview input harness actually supports.

## What I want

Research only — do not change the verified build yet. Determine:

1. **Can the Desktop Preview capture real keyboard input at all?** Investigate
   whether Lens Studio's Desktop Preview harness supports any of:
   - `TextInputField` receiving focus + the system keyboard (does it ever appear?).
   - A raw keyboard event path (e.g. any `InputSystem`/keyboard module, or a global
     text-input capture) that receives keystrokes in Preview.
   - The `TextInputSystem` API — does `showKeyboard()` / focus work in Preview, or is
     it device-only?
   - Any documented Desktop-Preview limitation on text input (check the CLAD field
     notes, LS docs, and the package source in `Cache/TypeScript/Src/Packages`).
2. **What is the authoritative answer for Spectacles on-device?** Confirm that on a
   real Spectacles device, tapping a `TextInputField` summons the on-screen keyboard
   and typing works (cite the API/package source that proves it).
3. **Recommend the best path** for a Desktop-Preview-demoable input:
   - If Preview CAN capture real keyboard, say exactly how and give a minimal
     repro/proof.
   - If it cannot, state that clearly with evidence, so I can decide between
     clickable vocabulary buttons (preview+device reliable) and a live inspector
     "type here" field (preview path, re-signs on edit).

## Success criteria (report each)

- A clear verdict: does Desktop Preview capture real keyboard input into a
  TextInputField? Yes/No with evidence (doc path, API name, or a minimal repro and
  its output).
- Confirmation (with source) that on-device typing works via the system keyboard.
- A recommendation with reasoning, and the exact API/code you would use for the
  chosen path.

## Report back

- The evidence for each verdict (file paths, API names, doc quotes, or repro output).
- Any minimal test code you ran and its Logger output.
- Your recommendation for the input mechanism, with the concrete API to use.
- Do NOT modify the verified build or commit anything — this is research only.